```

//reverse the string
String rev = new StringBuilder(words[i]).reverse().toString();

//string s1=kaji tempChar=ji after index i
int index=s1.indexOf(tempChar,i);
//index==>2

//s1==s2
s1.equals(s2);

You're right - the append() method in StringBuilder can directly accept integers, so we don't need to convert them to strings. Let me update the code to be more concise..
ex,,, int a =1;
      stringBuilder b = new StringBuilder('2');

  b.append(a); =>  '21'

```

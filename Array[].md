```

 List<Integer> indices = new ArrayList<>();

                (To make List into Array)

 indices.stream().mapToInt(Integer::intValue).toArray();
   (int[])


//l1 is array int[] l1 = new int[n];
Arrays.sort(l1,(a,b)->Integer.compare(a[0],b[0]));


```
